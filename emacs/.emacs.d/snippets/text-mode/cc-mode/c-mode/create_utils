int	xstrlen(char *str)
{
  int   n;

  n = 0;
  while (str != NULL && str[n])
    n++;
  return (n);
}

int     xstrcmp(char *s1, char *s2)
{
  int   n;

  n = 0;
  while (s1[n] == s2[n] && s1[n] != '\0')
    n++;
  return (s1[n] - s2[n]);
}

